# Server Configuration
# Default: localhost:8080 (local only - secure by default)
# Production: Use :8080 to listen on all interfaces or specify IP like 192.168.1.100:8080
SERVER_ADDRESS=localhost:8080

# Logging Configuration
# Log level: debug, info, warn, error
LOG_LEVEL=info
# Log format: json (production), text (development)
LOG_FORMAT=text

# Supabase Configuration (Required)
SUPABASE_URL=
SUPABASE_KEY=

# Authentication Configuration
# Base URL for auth redirects (used for email confirmation and password reset links)
# Default: http://localhost:8080 (for development)
# Production: Set to your domain (e.g., https://yourdomain.com)
AUTH_REDIRECT_URL=http://localhost:8080

# Whether to set Secure flag on cookies (requires HTTPS)
# Default: false (for local development)
# Production: Set to true
AUTH_COOKIE_SECURE=false

# Session cookie max age in seconds
# Default: 3600 (1 hour)
AUTH_ACCESS_TOKEN_MAX_AGE=3600

# Refresh token cookie max age in seconds
# Default: 604800 (7 days)
AUTH_REFRESH_TOKEN_MAX_AGE=604800

# Cookie names (optional, use defaults if not set)
AUTH_SESSION_COOKIE_NAME=vibefeeder_session
AUTH_REFRESH_COOKIE_NAME=vibefeeder_refresh

# Registration code (optional, if set registration requires this code)
# Leave empty for open registration
AUTH_REGISTRATION_CODE=

# SMTP Configuration (Required for email notifications)
# SMTP server host (e.g., smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=
# SMTP server port (usually 587 for TLS, 465 for SSL)
SMTP_PORT=587
# SMTP username (usually your email address or API key name)
SMTP_USER=
# SMTP password or API key
SMTP_PASSWORD=
# Email address to use as sender
SMTP_ADMIN_EMAIL=
# Display name for the sender
SMTP_SENDER_NAME=VibeFeeder

# OpenRouter AI Configuration (Required)
# Get your API key from https://openrouter.ai
OPENROUTER_API_KEY=

# Rate Limiting Configuration
# Minimum interval between summary generation requests per user (in seconds)
# Default: 30 (for testing, use 300 for production - 5 minutes)
RATE_LIMIT_SUMMARY_INTERVAL=30

# Fetcher Configuration
# How often to check for feeds to fetch (in seconds)
# Default: 300 (5 minutes)
FETCHER_INTERVAL=300

# Minimum interval after successful fetch (in seconds)
# Default: 3600 (1 hour)
FETCHER_SUCCESS_INTERVAL=3600

# Number of concurrent workers
# Default: 10
FETCHER_WORKERS=10

# Maximum number of feeds to process per batch
# Default: 1000
FETCHER_BATCH_SIZE=1000

# Delay between requests to same domain (in seconds)
# Default: 3
FETCHER_DOMAIN_DELAY=3

# Timeout for HTTP request (in seconds)
# Default: 30
FETCHER_REQUEST_TIMEOUT=30

# Timeout for entire job (in seconds)
# Default: 45
FETCHER_JOB_TIMEOUT=45

# Maximum number of articles to save per feed
# Default: 100
FETCHER_MAX_ARTICLES=100

# Maximum response body size in MB
# Default: 2
FETCHER_MAX_BODY_SIZE_MB=2
