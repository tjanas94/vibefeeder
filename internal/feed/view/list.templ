package view

import (
	"fmt"
	"github.com/tjanas94/vibefeeder/internal/feed/models"
)

// List renders the feed list component (debug version)
templ List(vm models.FeedListViewModel) {
	<div class="p-4">
		<h1 class="text-2xl font-bold mb-4">internal/feed/view/list.templ</h1>
		<pre>{ fmt.Sprintf("%+v", vm) }</pre>
	</div>
}
