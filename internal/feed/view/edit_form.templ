package view

import (
	"fmt"
	"github.com/tjanas94/vibefeeder/internal/feed/models"
)

// FeedEditForm renders the feed edit form component (debug version)
templ FeedEditForm(vm models.FeedEditFormViewModel) {
	<div class="p-4">
		<h1 class="text-2xl font-bold mb-4">internal/feed/view/edit_form.templ</h1>
		<pre>{ fmt.Sprintf("%+v", vm) }</pre>
	</div>
}
