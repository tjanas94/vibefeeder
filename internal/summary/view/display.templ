package view

import (
	"fmt"
	"github.com/tjanas94/vibefeeder/internal/summary/models"
)

// Display renders the summary display component (debug version)
templ Display(vm models.SummaryDisplayViewModel) {
	<div class="p-4">
		<h1 class="text-2xl font-bold mb-4">internal/summary/view/display.templ</h1>
		<pre>{ fmt.Sprintf("%+v", vm) }</pre>
	</div>
}
